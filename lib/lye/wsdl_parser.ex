defmodule Lye.WSDLParser do
  def parse(description) do
  end

  defp parse_protocol(wsdl, raw_description) do
  end

  defp parse_service(wsdl, raw_description) do
  end

  defp parse_interfaces(wsdl, raw_description) do
  end

  defp parse_endpoints(wsdl, raw_description) do
  end

  defp parse_bindings(wsdl, raw_description) do
  end
end
